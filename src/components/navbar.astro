<nav x-data="{ userMenuOpen: false, mobileMenuOpen: false, productsOpen: false, resourcesOpen: false, companyOpen: false }" id="navbar">
  <div class="relative text-gray-10">
    <div class="flex items-center">
      <!-- Mobile menu button - LEFT of logo -->
      <button
        @click="mobileMenuOpen = !mobileMenuOpen"
        class="sm:hidden flex items-center justify-center h-12 p-3 hover:text-gray hover:bg-gray-90"
        aria-label="Menu"
        :aria-expanded="mobileMenuOpen"
      >
        <img src="/navbar/menu.svg" alt="Menu" class="w-6 h-6" />
      </button>

      <!-- Logo -->
      <div class="flex hover:bg-gray-90 px-3 py-[6px]">
        <a href="/" id="nav-home" class="flex items-center">
          <img src="/mascot.svg" alt="QuackHost Mascot" class="h-9" />
          <p class="ml-3 hover:text-gray">QuackHost</p>
        </a>
      </div>

      <!-- Desktop navigation -->
      <nav
        class="hidden sm:flex items-center"
        role="navigation"
        aria-label="Main navigation"
      >
        <!-- Products Dropdown -->
        <div class="relative" @mouseenter="productsOpen = true" @mouseleave="productsOpen = false">
          <button
            @click="productsOpen = !productsOpen"
            class="hover:text-gray hover:bg-gray-90 flex items-center h-12 p-3"
            :class="{ 'bg-gray-90': productsOpen }"
            aria-haspopup="true"
            :aria-expanded="productsOpen"
          >
            Products
            <svg class="w-4 h-4 ml-1" :class="{ 'rotate-180': productsOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div
            x-show="productsOpen"
            x-transition:enter="transition ease-out duration-200"
            x-transition:enter-start="opacity-0 transform scale-95"
            x-transition:enter-end="opacity-100 transform scale-100"
            x-transition:leave="transition ease-in duration-150"
            x-transition:leave-start="opacity-100 transform scale-100"
            x-transition:leave-end="opacity-0 transform scale-95"
            class="absolute left-0 mt-0 w-64 bg-gray-90 shadow-md z-20"
            style="display: none;"
          >
            <a href="/games" class="block p-3 hover:text-gray hover:bg-gray-80">Game Servers</a>
            <a href="/quackplane" class="block p-3 hover:text-gray hover:bg-gray-80">QuackPlane Panel</a>
            <a href="/data-centers" class="block p-3 hover:text-gray hover:bg-gray-80">Data Centers</a>
            <a href="/pricing" class="block p-3 hover:text-gray hover:bg-gray-80">Pricing</a>
          </div>
        </div>

        <!-- Resources Dropdown -->
        <div class="relative" @mouseenter="resourcesOpen = true" @mouseleave="resourcesOpen = false">
          <button
            @click="resourcesOpen = !resourcesOpen"
            class="hover:text-gray hover:bg-gray-90 flex items-center h-12 p-3"
            :class="{ 'bg-gray-90': resourcesOpen }"
            aria-haspopup="true"
            :aria-expanded="resourcesOpen"
          >
            Resources
            <svg class="w-4 h-4 ml-1" :class="{ 'rotate-180': resourcesOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div
            x-show="resourcesOpen"
            x-transition:enter="transition ease-out duration-200"
            x-transition:enter-start="opacity-0 transform scale-95"
            x-transition:enter-end="opacity-100 transform scale-100"
            x-transition:leave="transition ease-in duration-150"
            x-transition:leave-start="opacity-100 transform scale-100"
            x-transition:leave-end="opacity-0 transform scale-95"
            class="absolute left-0 mt-0 w-64 bg-gray-90 shadow-md z-20"
            style="display: none;"
          >
            <a href="/support" class="block p-3 hover:text-gray hover:bg-gray-80">Support Center</a>
            <a href="/guides-&-tutorials" class="block p-3 hover:text-gray hover:bg-gray-80">Guides & Tutorials</a>
            <a href="/blog" class="block p-3 hover:text-gray hover:bg-gray-80">Blog</a>
            <a href="/faq" class="block p-3 hover:text-gray hover:bg-gray-80">FAQ</a>
            <a href="/learn-more" class="block p-3 hover:text-gray hover:bg-gray-80">Documentation</a>
            <a href="/service-status" class="block p-3 hover:text-gray hover:bg-gray-80">Service Status</a>
          </div>
        </div>

        <!-- Company Dropdown -->
        <div class="relative" @mouseenter="companyOpen = true" @mouseleave="companyOpen = false">
          <button
            @click="companyOpen = !companyOpen"
            class="hover:text-gray hover:bg-gray-90 flex items-center h-12 p-3"
            :class="{ 'bg-gray-90': companyOpen }"
            aria-haspopup="true"
            :aria-expanded="companyOpen"
          >
            Company
            <svg class="w-4 h-4 ml-1" :class="{ 'rotate-180': companyOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div
            x-show="companyOpen"
            x-transition:enter="transition ease-out duration-200"
            x-transition:enter-start="opacity-0 transform scale-95"
            x-transition:enter-end="opacity-100 transform scale-100"
            x-transition:leave="transition ease-in duration-150"
            x-transition:leave-start="opacity-100 transform scale-100"
            x-transition:leave-end="opacity-0 transform scale-95"
            class="absolute left-0 mt-0 w-64 bg-gray-90 shadow-md z-20"
            style="display: none;"
          >
            <a href="/about" class="block p-3 hover:text-gray hover:bg-gray-80">About Us</a>
            <a href="/roadmap" class="block p-3 hover:text-gray hover:bg-gray-80">Roadmap</a>
            <a href="/case-studies" class="block p-3 hover:text-gray hover:bg-gray-80">Case Studies</a>
            <a href="/affiliates" class="block p-3 hover:text-gray hover:bg-gray-80">Affiliate Program</a>
            <a href="/discord-flock" class="block p-3 hover:text-gray hover:bg-gray-80">Community</a>
            <a href="/contact" class="block p-3 hover:text-gray hover:bg-gray-80">Contact</a>
          </div>
        </div>
      </nav>

      <!-- User icon -->
      <div class="flex items-center ml-auto">
        <div class="relative">
          <button
            @click="userMenuOpen = !userMenuOpen"
            class="flex items-center justify-center h-12 p-3 hover:text-gray"
            :class="{ 'bg-gray-90 hover:bg-gray-80': userMenuOpen, 'hover:bg-gray-90': !userMenuOpen }"
            aria-label="User menu"
            :aria-expanded="userMenuOpen"
            aria-haspopup="true"
          >
            <img src="/navbar/user.svg" alt="User" class="w-6 h-6" />
          </button>

          <div
            x-show="userMenuOpen"
            @click.away="userMenuOpen = false"
            id="user-dropdown"
            class="absolute right-0 mt-0 w-64 bg-gray-90 shadow-md z-20"
            role="menu"
            style="display: none;"
          >
            <a
              href="https://client.quack.host"
              role="menuitem"
              class="block p-3 hover:text-gray hover:bg-gray-80"
              tabindex="-1">Client Portal</a
            >
            <a
              href="https://plane.quack.host"
              role="menuitem"
              class="block p-3 hover:text-gray hover:bg-gray-80"
              tabindex="-1">QuackPlane</a
            >
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile menu -->
    <div
      x-show="mobileMenuOpen"
      @click.away="mobileMenuOpen = false"
      class="absolute top-12 left-0 right-0 bg-gray-90 w-full max-w-sm z-50 shadow-md sm:hidden"
      style="display: none;"
    >
      <nav>
        <!-- Products Section -->
        <div x-data="{ mobileProductsOpen: false }">
          <button
            @click="mobileProductsOpen = !mobileProductsOpen"
            class="w-full flex items-center justify-between p-3 hover:text-gray hover:bg-gray-80"
          >
            <span>Products</span>
            <svg class="w-4 h-4" :class="{ 'rotate-180': mobileProductsOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div x-show="mobileProductsOpen" class="bg-gray-80" style="display: none;">
            <a href="/games" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Game Servers</a>
            <a href="/quackplane" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">QuackPlane Panel</a>
            <a href="/data-centers" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Data Centers</a>
            <a href="/pricing" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Pricing</a>
          </div>
        </div>

        <!-- Resources Section -->
        <div x-data="{ mobileResourcesOpen: false }">
          <button
            @click="mobileResourcesOpen = !mobileResourcesOpen"
            class="w-full flex items-center justify-between p-3 hover:text-gray hover:bg-gray-80"
          >
            <span>Resources</span>
            <svg class="w-4 h-4" :class="{ 'rotate-180': mobileResourcesOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div x-show="mobileResourcesOpen" class="bg-gray-80" style="display: none;">
            <a href="/support" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Support Center</a>
            <a href="/guides-&-tutorials" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Guides & Tutorials</a>
            <a href="/blog" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Blog</a>
            <a href="/faq" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">FAQ</a>
            <a href="/learn-more" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Documentation</a>
            <a href="/service-status" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Service Status</a>
          </div>
        </div>

        <!-- Company Section -->
        <div x-data="{ mobileCompanyOpen: false }">
          <button
            @click="mobileCompanyOpen = !mobileCompanyOpen"
            class="w-full flex items-center justify-between p-3 hover:text-gray hover:bg-gray-80"
          >
            <span>Company</span>
            <svg class="w-4 h-4" :class="{ 'rotate-180': mobileCompanyOpen }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>
          <div x-show="mobileCompanyOpen" class="bg-gray-80" style="display: none;">
            <a href="/about" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">About Us</a>
            <a href="/roadmap" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Roadmap</a>
            <a href="/case-studies" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Case Studies</a>
            <a href="/affiliates" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Affiliate Program</a>
            <a href="/discord-flock" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Community</a>
            <a href="/contact" class="block p-3 pl-6 hover:text-gray hover:bg-gray-70">Contact</a>
          </div>
        </div>
      </nav>
    </div>
  </div>

  <script>
    // Dropdown navigation state
    let currentDropdownIndex = 0;
    let isKeyboardUser = false;

    // Detect if user is navigating with keyboard
    document.addEventListener("keydown", () => {
      isKeyboardUser = true;
    });

    document.addEventListener("mousedown", () => {
      isKeyboardUser = false;
    });

    function updateDropdownFocus() {
      const dropdownItems = document.querySelectorAll(
        '#user-dropdown [role="menuitem"]'
      );
      dropdownItems.forEach((item, index) => {
        const htmlItem = item as HTMLElement;
        htmlItem.tabIndex = index === currentDropdownIndex ? 0 : -1;
        // Only focus if user is using keyboard
        if (index === currentDropdownIndex && isKeyboardUser) {
          htmlItem.focus();
        }
      });
    }

    function navigateDropdown(direction: string) {
      const dropdown = document.getElementById("user-dropdown");
      if (!dropdown || dropdown.style.display === "none") return;

      const dropdownItems = document.querySelectorAll(
        '#user-dropdown [role="menuitem"]'
      );
      if (direction === "down") {
        currentDropdownIndex =
          (currentDropdownIndex + 1) % dropdownItems.length;
      } else if (direction === "up") {
        currentDropdownIndex =
          (currentDropdownIndex - 1 + dropdownItems.length) %
          dropdownItems.length;
      }
      updateDropdownFocus();
    }

    // Global keyboard shortcuts
    document.addEventListener("keydown", (e) => {
      const key = e.key.toLowerCase();

      // 0 = home page
      if (key === "0" && !e.ctrlKey && !e.metaKey && !e.altKey) {
        e.preventDefault();
        document.getElementById("nav-home").click();
        return;
      }

      // L = toggle login dropdown
      if (key === "l" && !e.ctrlKey && !e.metaKey && !e.altKey) {
        e.preventDefault();
        const userMenuBtn = document.querySelector(
          '[aria-label="User menu"]'
        ) as HTMLElement;
        if (userMenuBtn) userMenuBtn.click();
        return;
      }

      // C = client portal
      if (key === "c" && !e.ctrlKey && !e.metaKey && !e.altKey) {
        e.preventDefault();
        window.location.href = "https://client.quack.host";
        return;
      }

      // Q = quackplane
      if (key === "q" && !e.ctrlKey && !e.metaKey && !e.altKey) {
        e.preventDefault();
        window.location.href = "https://plane.quack.host";
        return;
      }

      // Arrow keys for dropdown navigation
      const dropdown = document.getElementById("user-dropdown");
      if (
        dropdown &&
        dropdown.style.display !== "none" &&
        ["arrowdown", "arrowup"].includes(key)
      ) {
        e.preventDefault();
        navigateDropdown(key === "arrowdown" ? "down" : "up");
        return;
      }

      // Enter to activate focused dropdown item
      if (dropdown && dropdown.style.display !== "none" && key === "enter") {
        e.preventDefault();
        const dropdownItems = document.querySelectorAll(
          '#user-dropdown [role="menuitem"]'
        );
        const targetItem = dropdownItems[currentDropdownIndex] as HTMLElement;
        if (targetItem) targetItem.click();
        return;
      }
    });

    // Reset dropdown index when menu opens
    document.addEventListener("click", (e) => {
      const target = e.target as HTMLElement;
      if (target && target.closest('[aria-label="User menu"]')) {
        setTimeout(() => {
          currentDropdownIndex = 0;
          updateDropdownFocus();
        }, 10);
      }
    });
  </script>
</nav>
